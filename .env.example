DB_USER=postgres
DB_PASSWORD=secretcircle
DB_HOST=localhost
DB_PORT=5434
DB_NAME=exerciseapp_db

# Full connection string (if using Prisma or similar)
# Note: actual .env files must use full static strings, env var interpolation does not work
DATABASE_URL=postgresql://postgres:secretcircle@localhost:5434/exerciseapp

NEXT_PUBLIC_BASE_URL=http://localhost:3000/

MEDIA_SERVER_URL=http://localhost:8081/

NEXT_PUBLIC_MEDIA_SERVER_THUMBNAILS=${MEDIA_SERVER_URL}media/thumbnails/
NEXT_PUBLIC_MEDIA_SERVER_VIDEOS=${MEDIA_SERVER_URL}media/videos/
